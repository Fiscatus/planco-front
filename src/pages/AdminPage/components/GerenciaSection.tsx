import { Box, Typography } from '@mui/material';

const GerenciaSection = () => {
  return (
    <Box sx={{ display: 'flex', flexDirection: 'column', gap: 2 }}>
      <Typography variant='h6'>Gerências</Typography>
    </Box>
  );
};

export { GerenciaSection };
